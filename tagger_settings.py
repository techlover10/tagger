# Array that saves we might not want to tag
TAGGER_IGNORE = ['\'', '-', '^', '&', '>', '`', '*', '<', '[']
# Array that saves NLTK tag types that we might not want to tag
# To tag everything, simply change these arrays to empty arrays
TAGGER_TYPE_IGNORE = ['$', '\'', '(', ')', ',', '--', '.', ':', 'SYM', '``', '\'\'', '#']
